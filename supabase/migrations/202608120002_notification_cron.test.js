import assert from'node:assert/strict';import{readFileSync}from'node:fs';import test from'node:test';const sql=readFileSync(new URL('./202608120002_notification_cron.sql',import.meta.url),'utf8');test('uses Vault without literal secrets',()=>{assert.match(sql,/vault\.decrypted_secrets/i);assert.doesNotMatch(sql,/https:\/\/[a-z0-9-]+\.supabase\.co/i);assert.doesNotMatch(sql,/service_role|anon_key/i)})
