import assert from'node:assert/strict';import test from'node:test';import{createNotificationHandler}from'./handler.js'
test('rejects unauthorized requests',async()=>{const h=createNotificationHandler({env:{CRON_SECRET:'secret'}});assert.equal((await h(new Request('http://x'))).status,401)})
test('sends with Key auth and counts message ids',async()=>{const calls=[];const h=createNotificationHandler({env:{CRON_SECRET:'secret',ONESIGNAL_APP_ID:'app',ONESIGNAL_REST_API_KEY:'key'},repository:{candidates:async()=>[{userId:'u',title:'T',body:'B',url:'/day',idempotencyKey:'00000000-0000-4000-8000-000000000000'}],markSent:async()=>{}},fetch:async(url,o)=>{calls.push(o);return new Response(JSON.stringify({id:'msg'}),{status:200})}});const r=await h(new Request('http://x',{headers:{Authorization:'Bearer secret'}}));assert.equal((await r.json()).sent,1);assert.equal(calls[0].headers.Authorization,'Key key')})
