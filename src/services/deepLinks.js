const safeId = value => /^[A-Za-z0-9_-]+$/.test(value)
export function normalizeDeepLink(input) { if(typeof input!=='string'||[...input].some(char=>char.codePointAt(0)<32)||input.includes('\\')||input.startsWith('//')) return null; let path; try { path=decodeURIComponent(input) } catch{return null} if(path.includes('..')||/^https?:/i.test(path))return null; const url=new URL(path,'https://dayflow.local'); if(url.origin!=='https://dayflow.local')return null; const task=url.pathname.match(/^\/tasks\/([^/]+)$/); const focus=url.pathname.match(/^\/focus\/([^/]+)$/); if(task&&safeId(task[1]))return{pathname:'/dashboard',search:`?openTask=${encodeURIComponent(task[1])}`,hash:'#tasks'}; if(focus&&safeId(focus[1]))return{pathname:'/dashboard',search:`?focusTask=${encodeURIComponent(focus[1])}`,hash:'#focus'}; const tabs={ '/day':'today','/habits':'habits','/routines':'routines','/dashboard':url.hash.slice(1)||'today' }; if(!(url.pathname in tabs))return null; return{pathname:'/dashboard',search:url.search,hash:`#${tabs[url.pathname]}`} }
export function dashboardStateFromLocation(location){const p=new URLSearchParams(location.search); return{tab:location.hash?.slice(1)||'today',openTaskId:p.get('openTask'),focusTaskId:p.get('focusTask'),action:p.get('action')}}
