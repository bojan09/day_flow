import { useEffect,useRef } from'react'
const FOCUSABLE='input,textarea,select,button,[href],[tabindex]:not([tabindex="-1"])';let locks=0
export function useDialogA11y({open,onClose,panelRef}){const restore=useRef(null);useEffect(()=>{if(!open)return;restore.current=document.activeElement;locks++;document.body.style.overflow='hidden';const panel=panelRef.current;panel?.querySelector(FOCUSABLE)?.focus?.();const key=e=>{if(e.key==='Escape')onClose();if(e.key==='Tab'&&panel){const items=[...panel.querySelectorAll(FOCUSABLE)].filter(x=>!x.disabled);if(!items.length)return;const first=items[0],last=items.at(-1);if((e.shiftKey&&document.activeElement===first)||(!e.shiftKey&&document.activeElement===last)){e.preventDefault();(e.shiftKey?last:first).focus()}}};window.addEventListener('keydown',key);return()=>{window.removeEventListener('keydown',key);restore.current?.focus?.();locks--;if(!locks)document.body.style.overflow=''}},[open,onClose,panelRef])}
